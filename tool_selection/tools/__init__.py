"""
Tools package for the centralized tool registry.

This package serves as the root for all individual tool implementations.
Each tool should be organized into its own module within a relevant subdirectory
(e.g., `productivity`, `treasure_hunt`, `ecommerce`).
"""