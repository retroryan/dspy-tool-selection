# OpenAI Provider Configuration
# Copy this file to .env to use OpenAI models

# Select the provider
DSPY_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Available OpenAI models (uncomment to use):
OPENAI_MODEL=gpt-4o                          # Latest GPT-4 Omni (default)
# OPENAI_MODEL=gpt-4o-mini                   # Fast and cost-effective
# OPENAI_MODEL=gpt-4-turbo                   # High-intelligence model
# OPENAI_MODEL=gpt-4-turbo-preview           # Latest GPT-4 Turbo preview
# OPENAI_MODEL=gpt-4                         # Original GPT-4
# OPENAI_MODEL=gpt-3.5-turbo                 # Fast, cost-effective
# OPENAI_MODEL=gpt-3.5-turbo-16k            # Extended context window

# Reasoning models (OpenAI o1 series):
# OPENAI_MODEL=o1-preview                    # Advanced reasoning
# OPENAI_MODEL=o1-mini                       # Faster reasoning

# Common LLM settings (optional, these are defaults)
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1024

# Demo settings
DEMO_VERBOSE=true
DSPY_DEBUG=false